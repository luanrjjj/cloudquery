# CloudQuery Newrelic Source Plugin Configuration Reference

## Example

This example connects a single Newrelic account to a Postgres destination. The (top level) source spec section is described in the [Source Spec Reference](/docs/reference/source-spec).

import Configuration from "./_configuration.mdx";

<Configuration />

## Newrelic Spec

This is the (nested) spec used by the Newrelic source plugin.

- `accounts` ([]struct) 

  Specify which accounts to sync data from.
